<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.triplog.user.mapper.UserMapper">

    <resultMap id="UserResultMap" type="com.triplog.user.domain.User">
        <id property="id" column="id"/>
        <result property="email" column="email"/>
        <result property="password" column="password"/>
        <result property="nickname" column="nickname"/>
        <result property="profileImg" column="profile_img"/>
        <result property="createdAt" column="created_at"/>
    </resultMap>

    <select id="countByEmail" resultType="int">
        SELECT COUNT(*)
        FROM users
        WHERE email = #{email}
    </select>

    <select id="findByEmail" resultMap="UserResultMap">
        SELECT id, email, password, nickname, profile_img, created_at
        FROM users
        WHERE email = #{email}
    </select>

    <select id="findById" resultMap="UserResultMap">
        SELECT id, email, password, nickname, profile_img, created_at
        FROM users
        WHERE id = #{id}
    </select>

    <select id="findByIdForUpdate" resultMap="UserResultMap">
        SELECT id, email, password, nickname, profile_img, created_at
        FROM users
        WHERE id = #{id}
        FOR UPDATE
    </select>

    <select id="existsById" resultType="int">
        SELECT EXISTS(
            SELECT 1
            FROM users
            WHERE id = #{id}
        )
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id" keyColumn="id">
        INSERT INTO users (email, password, nickname, profile_img)
        VALUES (#{email}, #{password}, #{nickname}, #{profileImg})
    </insert>

    <update id="updateProfile">
        UPDATE users
        SET nickname = #{nickname},
            profile_img = #{profileImg}
        WHERE id = #{id}
    </update>

    <update id="updatePassword">
        UPDATE users
        SET password = #{password}
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM users
        WHERE id = #{id}
    </delete>
</mapper>
