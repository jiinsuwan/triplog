# 프론트엔드 환경변수 예시. 복사해서 .env 로 사용 (값은 각자 채움). .env 는 커밋 금지.
# Vite 는 VITE_ 접두사 변수만 클라이언트에 노출한다.

# 백엔드 API 베이스 URL
VITE_API_BASE_URL=http://localhost:8080

# 카카오맵 JavaScript(클라이언트) 키 — trip 트랙 지도 (architecture §8)
VITE_KAKAO_MAP_KEY=
