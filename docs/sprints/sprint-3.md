# Sprint 3 — 일정 & 카드 생성 → P0 완성

> 상태: ✅ 완료 (2026-06-22)
> 기간: 2026-06-12 ~ 2026-06-22

## 목표
> 일정(stop, 이동수단 포함) 위에서 여행 계획이 완성되고, **사진 → AI 카드 생성 → PNG 다운로드**가 end-to-end로 동작한다 (P0 완성). S2 회고에서 채택한 프로세스(상호 리뷰 + PR guardrail)가 가동된다.

## 지난 스프린트 회고 (이월 포함)
- **완료**: 관광지 탐색·상세 + 사진 파이프라인 + FE 화면 — 종료 조건 4/4 ([sprint-2.md](sprint-2.md) 회고).
- **이월/입력**: #17 잔여(status 검증·size) → `S3-CORE-03`(#67)에 흡수(#17은 #67 참조로 닫기) / #59 → `S3-CORE-01`(#65)로 구현(채택 코멘트 후 닫음) / #20(카드 실측) → 카드 동작 후 측정 / 카드 PoC v12 산출물([decisions/0006](../decisions/0006-card-outline-module.md) + [card-poc-v12-report §7](../poc/card-poc-v12-report.md)) = 카드 이슈 분해의 입력 / 주말 스파이크 산출물 발생 시 이슈 계약 후 포팅(poc 직접 머지 금지).

## 이번 스프린트 작업
> 각 항목 = GitHub Issue. **발행 분담(S2 회고): log 담당자 = core + log / trip 담당자 = trip.** 발행자가 본문(Goal/Scope/AC/Test) 작성, core Issue에는 회고 결정 근거를 명시한다.

### trip (전반부 우선 — S4 기록 뷰의 선행 작업)
- [x] **[S3-TRIP-01]** 일정(stop) 스키마 + CRUD API — 날짜·시간·장소·순서 + **이동수단(필수 필드)**, 사진 선택적 연결 고려([frontend-structure §3-3](../frontend-structure.md)). **스키마 = core 리뷰 필수**
- [x] **[S3-TRIP-02]** 일정 편집 화면 — 날짜별 장소 추가, 드래그앤드롭 정렬, 지도 핀 순서 동선 (기존 #29를 이 항목으로 재정의 가능 — trip 판단)
- [x] **[S3-TRIP-03]** 구간 소요시간 표시 — 자동차 = 카카오모빌리티 길찾기 API 연동 시도(#39 공통 인프라 활용), **미연동·실패 시 추정/수동 폴백이 P0 합격선(외부 API는 P0 blocker 아님)** / 도보·대중교통 = 추정·수동 입력. Notes: TMAP(보행)·ODsay(대중교통) 최신 제공 여부 확인. 실이동경로 = P1, 실시간 소요시간 = P2
- (자율) 장소 상세정보·컨텐츠 타입 확장 — 고려사항, 이슈화 여부 trip 판단

### log (카드 생성 — [0004](../decisions/0004-card-poc-result.md) + [0006](../decisions/0006-card-outline-module.md) 기반)
- [x] **[S3-LOG-01]** 카드 overlay 스키마 확정 — 객체별 짧은 문구 + 좌표·외곽선·장식 (0004 D1·D3, OUTLINE_API 정합)
- [x] **[S3-LOG-02]** 외곽선 사이드카 도입 — `outline_module`/`serve_outline` 제품 포팅, 업로드 1회 전처리 = **비동기 큐(업로드 차단 금지)** (0006 D1·D2 — SAM 인코딩 캐시 검토 + 처리 시간/RSS 측정 기준 포함)
- [x] **[S3-LOG-03]** 텍스트 LLM 어댑터 + 프롬프트 — 객체별 **짧은 문구만**, 배치 = OUTLINE_API §2-1 기반 (S3-CORE-02 위에서). **역할 경계: 좌표·앵커 후보 = 코드(사이드카) 산출, LLM = 후보 중 선택 + 문구 생성만 — 임의 좌표 생성 금지(0004 D3 유지)**
- [x] **[S3-LOG-04]** Canvas2D overlay 렌더링 — `legacy-v3/render-overlay.mjs` 포팅, 세로 1080×1920
- [x] **[S3-LOG-05]** 카드 PNG export (세로 1080×1920) — F07·F08
- [x] **[S3-LOG-06]** 카드 생성 화면 — 위저드: Trip 선택 → 사진 ≤10 → 자동 초안 → 에디터(탭/박스/그룹 버블 2종/톤다운 슬라이더 35%) → 미리보기 → PNG
- [x] **[S3-LOG-07]** 자동 초안 폴백·수동 보정 — **P0 합격선 = 초안 + 수동 보정으로 PNG** (0004 D6). #20 실측은 이 동작 후 별도 진행

### core (공통)
> 번호 = 발행 이슈 기준 01부터 연속 (2026-06-12 재정렬 — 빈 슬롯 제거). 실행 권장 순서 = 01(guardrail) 우선.
> **구현 분담(S2 회고 후 합의): CORE-01·02 = log 담당 / CORE-03·04 = trip 담당.** 어느 쪽이든 core = 상대 리뷰 필수(§1-3).

- [x] **[S3-CORE-01]** PR guardrail (#65) — 본문 검사 CI(feat·fix = required) + 공유 경로 감지(자동 코멘트 + `track:core` 라벨) + **reviewer 미지정 경고 코멘트** — **#59 채택 구현**. 선행 해소됨: required check 설정 가능 확인 완료(#65 코멘트 참조). 공유 경로 목록 확정(`api/config.js`·`user/` 포함 여부)도 이 이슈에서
- [x] **[S3-CORE-02]** AI 호출 공통 인프라 (#66) — `LlmAdapter`, provider 1개 = GMS 우선, AiCallLog (S2에서 이동)
- [x] **[S3-CORE-03]** IA 라우팅 + status 체계 (#67) — 계획/기록 2워크스페이스·상태 기반 진입([frontend-structure §3-0](../frontend-structure.md)) + Trip `status` 허용값·검증 + 목록 응답 `size` — **#17 잔여 흡수, 리뷰 필수**
- [x] **[S3-CORE-04]** 비밀번호 찾기 (#68) — 이메일 인프라 없는 방식 우선 확인. 성격 = **심사기준 보강(P0 아님)**, P0 지장 시 S4 이월
- (번호 미부여 · 재검토) 사진/카드 서빙 권한 보강 — S2에서 인증 엔드포인트 서빙(#38)으로 선반영. 잔여 범위 확인 후 필요 시 이슈화·번호 부여

## 종료 조건
- [x] **여행 → 사진 → AI 카드 생성 → PNG 다운로드 end-to-end 동작** (P0)
- [x] AI 응답(짧은 문구) 검증 테스트가 CI 통과
- [x] 일정 에디터에서 드래그앤드롭으로 날짜별 일정 구성 (이동수단 입력 포함)
- [x] PR guardrail CI 동작 (본문 검사 + 공유 경로 감지)

## 의존성·선결
- S3-TRIP-01(stop 스키마) → S4 기록 뷰(사진 배치) — trip 전반부 우선 배치의 사유. trip 완료 + log 여유 시 기록 뷰(S4-LOG-01) 앞당김 가능
- S3-CORE-02(LLM 인프라) → S3-LOG-03(어댑터·프롬프트)
- 카드 흐름(S3-LOG-*)은 일정과 독립 — 사진·여행 연결은 S2에 완비
- S3-CORE-03의 status 값 체계 = frontend-structure §3-3 가안 기준, 확정은 해당 이슈에서

## 회고 (2026-06-22)
- **완료**: 종료 조건 4/4 충족. 전 이슈 완료 — log #69(PR #84)·#70(#89)·#71(#90)·#72(#91)·#73(#99)·#74(#100)·#75(#101) / core #65(#80)·#66(#83)·#67(#82)·#68(#85) + 스프린트 중 추가 #87(#88)·#92(#93) / trip #77(#86)·#78(#94)·#79(#95). 여행 → 사진 → AI 카드 생성 → PNG 다운로드 end-to-end 동작.
- **이월/처분**: 미완 이월 없음. #20(카드 비용·속도 라이브 실측) = 불필요 판단 종료(0004 추정치 갈음) / 외곽선 보정(탭·박스로 미검출 객체 보강) = S4 이월(P1, core 성격 = 보정 결과 영속 스키마, 발행 시 경유 방식·영속 위치 확정) — PR #100에서 후속 약속됐으나 발행 누락분 / #16·#24 백로그 유지.
- **트랙 결정 요약 (서로 검토)**:
  - (log) 카드 비율 = **1080×1920 고정 폐기**. 렌더 = 원본 사진 풀블리드(크롭 없음), export = 원본 비율 기본 + 9:16 옵션(9:16 선택·비율 불일치 시 위/아래 블러(기본)/단색 패딩) → decisions/0004 D1 정합 갱신 / 자동 초안 폴백(#75) = 사이드카·LLM 불능 시 baseline + 수동 보정으로 PNG = P0 합격선 충족(0004 D6).
  - (core) CORE-05(#87) PR 리뷰·로컬 통합테스트 표준화 / CORE-06(#92) 진입 문서 정본화(AGENTS 슬림화·금지목록 통합·README 발견 진입점·권위 모델 일원화) — 둘 다 스프린트 중 발행·구현.
  - (trip) 구간 소요시간 = TMAP 채택(자동차·도보), 대중교통 = TMAP 일 10회 제한으로 수동 입력. 자동차·도보는 TMAP route geometry 지도 표시까지 포함(#95). 후속 #96·#97·#98 = Codex가 사람 합의 없이 발행 → 팀원이 닫음(미채택).
- **`proposal` Issue 검토 결과**: 스프린트 중 proposal로 발행된 #87·#92 채택·구현 완료. #59(S2 채택분, 가드레일 3건)은 CORE-01(#65)로 구현 완료.
- **배운 점 · 다음 입력**:
  1) PoC 고정 가정(카드 = 1080×1920)이 실제 사진(대부분 가로)에서 깨짐 — LOG-04 렌더 시연에서 드러나 원본 풀블리드로 피벗. PoC 가정은 실입력으로 조기 검증.
  2) 이슈 생애주기가 가드레일 밖 — 같은 스프린트에 양방향 사례: PR가 약속한 후속(외곽선 보정) 발행 누락(log) + Codex의 사람 합의 없는 이슈 무단 발행(trip, 팀원 회수). guardrail CI는 PR 본문 형식만 검사 — 이슈 발행·후속 추적은 사람이 메움. 다음 개선 후보.
  3) PoC 덤프·픽스처를 정본으로 신뢰 금지 — #71에서 PoC 덤프의 center/area를 정본 전제로 쓴 오류를 사람 리뷰가 잡음(자동 검사는 통과). 필드·스키마 정본 = 계약·생산자 코드.
  4) 스프린트 중 프로세스 보강 2건(CORE-05·06) — S2 회고의 "객관 규칙은 CI·문서 정본으로" 흐름 연장.
  - **다음 (S4 = P1)**: roadmap S4 기준 — log = 기록 뷰(F14, S4-LOG-01) + 카드 수동 편집(F12)·정사각 카드(F09), 외곽선 보정 = S4-LOG-02 인근 발행 검토 / trip = 즐겨찾기·챗봇·소셜로그인 / core = 회원 탈퇴(#50, S4-CORE-04)·제출 산출물(다이어그램·PPT·AI 사용 보고서, S4-CORE-05)·README·통합 점검. 비번찾기는 S3(#68) 완료. sprint-4.md 작성 + 이슈 분해(분담 = log 담당자 core+log / trip 담당자 trip).
  - **S4 보강 후보(리뷰 제안)**: Trip route 자동 테스트 하드닝 — FE = `travelFromPrevious.routePath` 있을 때 직선 fallback이 아니라 실제 geometry 우선 렌더 / BE = `travelGeometryJson`→`TravelEstimateResponse.routePath` 변환 계약 / API = `public_transit` 수동 입력 후 `status=manual`·`durationSeconds` 반영. 테스트 구현은 S4 또는 별도 Trip 품질보강 PR(이 PR엔 포함 안 함).
