<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.triplog.photo.mapper.PhotoMapper">

    <resultMap id="PhotoResultMap" type="com.triplog.photo.domain.Photo">
        <id property="id" column="id"/>
        <result property="userId" column="user_id"/>
        <result property="originalFilename" column="original_filename"/>
        <result property="storedFilename" column="stored_filename"/>
        <result property="contentType" column="content_type"/>
        <result property="sizeBytes" column="size_bytes"/>
        <result property="createdAt" column="created_at"/>
    </resultMap>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id" keyColumn="id">
        INSERT INTO photos (user_id, original_filename, stored_filename, content_type, size_bytes)
        VALUES (#{userId}, #{originalFilename}, #{storedFilename}, #{contentType}, #{sizeBytes})
    </insert>

    <select id="findById" resultMap="PhotoResultMap">
        SELECT id, user_id, original_filename, stored_filename, content_type, size_bytes, created_at
        FROM photos
        WHERE id = #{id}
    </select>
</mapper>
