<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.triplog.auth.mapper.PasswordResetTokenMapper">

    <resultMap id="PasswordResetTokenResultMap" type="com.triplog.auth.domain.PasswordResetToken">
        <id property="id" column="id"/>
        <result property="userId" column="user_id"/>
        <result property="tokenHash" column="token_hash"/>
        <result property="expiresAt" column="expires_at"/>
        <result property="consumedAt" column="consumed_at"/>
        <result property="revokedAt" column="revoked_at"/>
        <result property="createdAt" column="created_at"/>
    </resultMap>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id" keyColumn="id">
        INSERT INTO password_reset_tokens (user_id, token_hash, expires_at)
        VALUES (#{userId}, #{tokenHash}, #{expiresAt})
    </insert>

    <select id="findByHash" resultMap="PasswordResetTokenResultMap">
        SELECT id, user_id, token_hash, expires_at, consumed_at, revoked_at, created_at
        FROM password_reset_tokens
        WHERE token_hash = #{tokenHash}
    </select>

    <update id="revokeUnusedByUserId">
        UPDATE password_reset_tokens
        SET revoked_at = #{revokedAt}
        WHERE user_id = #{userId}
          AND consumed_at IS NULL
          AND revoked_at IS NULL
    </update>

    <update id="consumeByHash">
        UPDATE password_reset_tokens
        SET consumed_at = #{consumedAt}
        WHERE token_hash = #{tokenHash}
          AND consumed_at IS NULL
          AND revoked_at IS NULL
          AND expires_at > #{consumedAt}
    </update>
</mapper>
